info:
  name: eventStatsAdmin
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/api/reports/events/1/stats"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
