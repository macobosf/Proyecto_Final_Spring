info:
  name: registrationsExcelAdmin
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/api/reports/events/1/registrations.xlsx"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  params:
    - name: ""
      value: ""
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
