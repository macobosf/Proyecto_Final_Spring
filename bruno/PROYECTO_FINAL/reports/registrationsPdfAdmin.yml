info:
  name: registrationsPdfAdmin
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/api/reports/events/1/registrations.pdf"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
