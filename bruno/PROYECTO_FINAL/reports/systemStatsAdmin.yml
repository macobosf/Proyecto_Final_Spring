info:
  name: systemStatsAdmin
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/api/reports/stats"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
