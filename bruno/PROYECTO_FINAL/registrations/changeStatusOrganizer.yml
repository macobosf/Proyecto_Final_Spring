info:
  name: changeStatusOrganizer
  type: http
  seq: 5

http:
  method: PATCH
  url: "{{baseUrl}}/api/events/1/registrations/49/status"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  body:
    type: json
    data: '{ "status": "CONFIRMED" }'
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
