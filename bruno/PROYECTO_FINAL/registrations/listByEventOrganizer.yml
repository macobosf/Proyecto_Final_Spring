info:
  name: listByEventOrganizer
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/api/events/1/registrations"
  headers:
    - name: Authorization
      value: " Bearer {{accessToken}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
