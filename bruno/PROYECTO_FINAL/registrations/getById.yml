info:
  name: getById
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/api/registrations/49"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
