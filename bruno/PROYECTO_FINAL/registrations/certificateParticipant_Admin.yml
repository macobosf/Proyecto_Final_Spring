info:
  name: certificateParticipant_Admin
  type: http
  seq: 6

http:
  method: GET
  url: "{{baseUrl}}/api/registrations/49/certificate.pdf"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
