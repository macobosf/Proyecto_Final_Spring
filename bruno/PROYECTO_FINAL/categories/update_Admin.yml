info:
  name: update_Admin
  type: http
  seq: 5

http:
  method: PUT
  url: "{{baseUrl}}/api/categories/9"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  body:
    type: json
    data: |-
      {
        "name": "Tecnología Nueva",
        "description": "Eventos relacionados a tecnología"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
