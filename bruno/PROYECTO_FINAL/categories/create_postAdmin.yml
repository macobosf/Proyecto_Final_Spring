info:
  name: create_postAdmin
  type: http
  seq: 9

http:
  method: POST
  url: "{{baseUrl}}/api/categories"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  body:
    type: json
    data: |-
      {
        "name": "Tecnología",
        "description": "Eventos relacionados a tecnología"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
