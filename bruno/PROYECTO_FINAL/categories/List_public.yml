info:
  name: List_public
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/api/categories"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
