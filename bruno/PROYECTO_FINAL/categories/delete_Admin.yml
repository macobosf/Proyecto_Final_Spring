info:
  name: delete_Admin
  type: http
  seq: 6

http:
  method: DELETE
  url: "{{baseUrl}}/api/categories/9"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
