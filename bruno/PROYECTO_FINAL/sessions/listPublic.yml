info:
  name: listPublic
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/api/events/1/sessions"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
