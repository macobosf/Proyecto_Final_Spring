info:
  name: deleteOrganizerSessions
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{baseUrl}}/api/events/1/sessions/23"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
