info:
  name: ListPrivSessions
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/api/events/1/sessions"
  headers:
    - name: Authorization
      value: "Bearer {{accessToken}} "
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
