info:
  name: deleteOrganizer
  type: http
  seq: 8

http:
  method: DELETE
  url: "{{baseUrl}}/api/events/14"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
