info:
  name: changeSatusOrganizer
  type: http
  seq: 11

http:
  method: PATCH
  url: "{{baseUrl}}/api/events/14/status"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  body:
    type: json
    data: |
      { "status": "PUBLISHED" }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
